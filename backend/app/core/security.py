"""Password hashing, JWT и cookie — реализация в Этапе 2 (см. PROJECT_SPEC.md §15).

На этом этапе модуль существует как точка подключения будущей логики auth.
"""

from app.core.config import get_settings

settings = get_settings()
